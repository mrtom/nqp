define([
  // Libraries.
  "jquery",
  "underscore",
  "backbone",

  // Models
  "models/user"
],

function($, _, Backbone, User) {

  // Nav Model
  // ----------

  var Nav = Backbone.Model.extend({

  });

  return Nav;

});
