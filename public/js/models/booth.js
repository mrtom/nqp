define([
  // Libraries.
  "jquery",
  "underscore",
  "backbone"
],

function($, _, Backbone) {

  // Booth Model
  // ----------

  var Booth = Backbone.Model.extend({

    initialize: function() {

    }
  });

  return Booth;

});
